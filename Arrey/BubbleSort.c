#include<stdio.h>
int main(){
    int limit;
    scanf("%d",&limit);
    int nums[limit];
    for(int i=0;i<limit;i++){
        nums[i];
        scanf("%d",&nums[i]);
    }
    for(int i=0;i<limit;i++){
        printf("%d",nums[i]);
    }
    

}